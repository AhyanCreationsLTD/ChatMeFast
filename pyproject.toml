[project]
name = "chat-me-fast"
version = "0.1.0"
description = "Anonymous Telegram Chat Bot"
requires-python = ">=3.12"
dependencies = [
    "aiogram==2.25.1",
    "aiohttp>=3.9.3",
    "Flask==3.0.2"
]

[tool.vercel]
entrypoint = "bot:app"
